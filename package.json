{
  "name": "telegram_bot",
  "version": "0.0.1",
  "license": "MIT",
  "scripts": {
    "prepare": "husky install",
    "client": "cd client && yarn dev",
    "server": "cd server && yarn dev"
  },
  "dependencies": {},
  "devDependencies": {
    "husky": "^8.0.0"
  }
}
